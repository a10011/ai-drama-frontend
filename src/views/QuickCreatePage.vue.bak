<template>
  <div class="quick-create-page">
    <div class="placeholder-card">
      <div class="placeholder-icon">🪄</div>
      <h2 class="placeholder-title">极简一键模式</h2>
      <p class="placeholder-text">开发中，敬请期待</p>
      <div v-if="prompt" class="query-info">
        <span class="query-label">输入提示词：</span>
        <span class="query-value">{{ prompt }}</span>
      </div>
      <div v-if="template" class="query-info">
        <span class="query-label">选中题材：</span>
        <span class="query-value">{{ template }}</span>
      </div>
    </div>
  </div>
</template>

<script>
export default {
  name: 'QuickCreatePage',
  data() {
    return {
      prompt: '',
      template: ''
    }
  },
  mounted() {
    this.prompt = this.$route.query.prompt || ''
    this.template = this.$route.query.template || ''
  }
}
</script>

<style scoped>
.quick-create-page {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 60vh;
}

.placeholder-card {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: 16px;
  padding: 48px;
  text-align: center;
  max-width: 480px;
  width: 100%;
}

.placeholder-icon {
  font-size: 56px;
  margin-bottom: 16px;
}

.placeholder-title {
  font-size: 22px;
  font-weight: 700;
  color: var(--text-primary);
  margin-bottom: 8px;
}

.placeholder-text {
  font-size: 18px;
  color: var(--text-secondary);
  margin-bottom: 24px;
}

.query-info {
  margin-top: 12px;
  padding: 12px 16px;
  background: var(--bg-input);
  border-radius: var(--radius-sm);
  border: 1px solid var(--border);
  text-align: left;
}

.query-label {
  font-size: 13px;
  color: var(--text-muted);
}

.query-value {
  font-size: 14px;
  color: var(--highlight);
  margin-left: 4px;
}
</style>
